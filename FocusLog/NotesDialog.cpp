#include "NotesDialog.h"

NotesDialog::NotesDialog(QWidget *parent) : QDialog(parent)
{
    // notesInput = new QLineEdit(this);
    // dbManager = new DatabaseManager();

    // QPushButton *saveButton = new QPushButton("Save", this);
    // connect(saveButton, &QPushButton::clicked, this, &NotesDialog::onSaveNotes);
}

void NotesDialog::onSaveNotes()
{
    // QString notes = notesInput->text();
    // dbManager->logNotes(notes);
    // this->close();  // Закрываем окно после сохранения
}
